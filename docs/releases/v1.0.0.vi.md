# v1.0.0 — Tìm kiếm ngữ nghĩa song ngữ với Qwen3-Embedding-4B + Qdrant
> 🌐 Language / Ngôn ngữ: [English](v1.0.0.md) | **Tiếng Việt**

## Tóm tắt

`v1.0.0` là release công khai đầu tiên của `nodejs-qdrant-bilingual-search`: API tìm kiếm ngữ nghĩa địa lý mở tiếng Anh/Việt được xây dựng với Node.js, Hono, Qdrant và Qwen3-Embedding-4B.

profile di động được chấp nhận chạy embedding model với **Transformers / PyTorch / CPU / FP16** trong khi hiển thị **Float32[2560]** vectors trên `binary-f32` transport được chuẩn hóa. canonical Qdrant snapshot chứa 20.000 thực thể đã được xác minh.

## Lịch sử và nguồn gốc bản phát hành

Lịch sử repository đã được tái cấu trúc để dễ xem xét hơn sau lần phát hành `v1.0.0` ban đầu. Tag ban đầu trỏ tới lịch sử cũ. Sau khi nhánh `main` tái cấu trúc được xác thực độc lập, `v1.0.0` được chuyển sang commit hoàn tất tài liệu cuối cùng trong dòng lịch sử mới.

Thao tác chuyển tag không tự nó chứng minh bất kỳ tuyên bố ngữ nghĩa hay runtime nào. Bằng chứng Kaggle production-demo sạch bên dưới được tạo từ source commit tái cấu trúc `b316619ad94947571e91124adfe96071bbd1f255` và tree `ff28454332cf976d1fb8fb17262924b09ca92cd2`. Các corrective sau đó chỉ giới hạn ở cách gọi Qwen3-Embedding-4B trong nội dung publication-facing, các ô Markdown giải thích trong notebook, metadata governance của repository, các status badge trên README dành cho reviewer và wording về metadata/provenance của release. Chúng không thay đổi hợp đồng runtime của ứng dụng, semantic profile, Qdrant snapshot, acceptance criteria hoặc provenance của runtime evidence đã được ghi nhận. Commit release cuối cùng chỉ được gắn tag sau khi chính commit đó chạy GitHub Actions thành công.

## Điểm nổi bật của Release

- Truy xuất ngữ nghĩa tiếng Anh/tiếng Việt trên kho ngữ liệu địa lý mở canonical 20K.
- `Qwen/Qwen3-Embedding-4B`, 2560 kích thước, khoảng cách cosin.
- Kaggle CPU-FP16 runtime di động với read-only model đang tải từ `/kaggle/input`.
- embedding vectors công khai được chuẩn hóa `Float32[2560]`.
- Node.js CI có thể tái tạo trên Node 22 và Node 24.
- Kiểm tra công cụ nhúng Python và kiểm tra tích hợp Qdrant thực.
- Tập dữ liệu xác định, seeding, công cụ xác minh và bằng chứng.
- Repository governance với CODEOWNERS, hướng dẫn đóng góp/hỗ trợ song ngữ, security policy, issue/PR template và cấu hình cập nhật dependency.
- Kỹ thuật/tham chiếu `runtime/true-fp32` riêng biệt branch.

## Kiến trúc

```text
read-only Qwen3-Embedding-4B model in /kaggle/input
        ↓
Python FastAPI embedding service
        ↓
normalized Float32[2560] / binary-f32
        ↓
Node.js + Hono search API
        ↓
Qdrant canonical 20K collection
```

endpoints cục bộ mặc định:

```text
embedding : 127.0.0.1:8001
Node/Hono : 127.0.0.1:3000
Qdrant    : 127.0.0.1:6333
```

## Hợp đồng runtime chuẩn

```text
model                = Qwen/Qwen3-Embedding-4B
backend              = transformers
implementation       = python-fastapi
runtime              = pytorch-cpu
device               = cpu
accelerator          = cpu
internal dtype       = float16
dimension            = 2560
public vector dtype  = float32
transport            = binary-f32
```

Đường ống chính xác:

```text
FP16 model forward
→ last-token pooling
→ cast pooled tensor to Float32
→ Float32 L2 normalization
→ Float32[2560] public vector
→ binary-f32 transport
```

## Hợp đồng ngữ nghĩa Canonical

```text
model                = Qwen/Qwen3-Embedding-4B
dimension            = 2560
profile              = qwen3
query_strategy       = prompt
document_strategy    = raw
query_instruction_id = geo-retrieval-v1:d014d3ec6df87e49
embedding_text       = v2.1
```

Lời nhắc query chính xác:

```text
Instruct: Retrieve the geographic entity that best answers the query
Query:
```

## Trạng thái Canonical Qdrant

```text
collection        = knowledge_entities_qwen3_4b_text_v21
points_count      = 20000
indexed_vectors   = 20000
vector size       = 2560
distance          = Cosine
status            = green
optimizer_status  = ok
```

Chính sách snapshot được chấp nhận:

```text
RESEED = NO
SNAPSHOT_REUSE = APPROVED
```

Việc tái sử dụng Snapshot được kiểm soát bằng xác minh danh tính ngữ nghĩa thay vì chỉ nhận dạng đường dẫn lưu trữ.

## Bằng chứng xác nhận

```text
semantic verifier       = 20000 / 20000 PASS
Node full tests          = 454 / 454 PASS
Node 22                  = PASS
Node 24                  = PASS
HTTP                     = 11 / 11 PASS
npm audit (full/prod)    = 0 vulnerabilities
OOM                      = 0
OOM_KILL                 = 0
```

### Hoàn tất bằng chứng Kaggle production-demo

Gate phát hành được xem xét độc lập vào ngày 2026-08-31 từ một lần Kaggle **Khởi động lại phiên → Chạy tất cả** sạch:

```text
runtime evidence source = b316619ad94947571e91124adfe96071bbd1f255
runtime evidence tree   = ff28454332cf976d1fb8fb17262924b09ca92cd2
evidence archive        = nodejs-qdrant-v1.0.0-production-demo-evidence-20260831T105116Z.zip
evidence SHA-256        = 3ab21f5d05dc8188d543167dce806b28144f2d2e6cc780e4eab0bdb895f7037a
snapshot restore        = PASS
reseed                   = NO
local acceptance         = PASS (7 / 7)
authenticated public     = NOT_RUN
evidence re-extract      = PASS
secret scan              = PASS
```

Do đường hầm công khai tùy chọn không được chạy, release này không tuyên bố kết quả runtime cho authenticated public acceptance. Hành vi gateway và public topology vẫn là các hợp đồng đã được kiểm tra trong CI.

Đánh giá lịch sử đầy đủ 20K v2.1 đạt xấp xỉ:

```text
R@1 = 96.25%
R@3 = 100%
R@5 = 100%
```

Đây là các kết quả đánh giá lịch sử cho profile ngữ nghĩa cố định, không phải là tuyên bố về mọi phân phối query có thể có.

## Khả năng tương thích ổn định sentinels

```text
Thailand EN = PASS
Tokyo VI    = PASS
Beijing VI  = PASS
```

Một bộ chẩn đoán kiểu quan hệ chặt chẽ hơn vẫn được cố ý đặt trong phạm vi chỉ dành cho chẩn đoán:

- Dạng viết hoa VI của Thái Lan có thể tạo ra sự gần như hòa giữa thành phố Bangkok và quốc gia Thái Lan, do đó, quốc gia được mong đợi không nhất thiết phải xếp hạng số 1 sau tính nhất quán loại thực thể nghiêm ngặt gate.
- Fuji/Nhật Bản EN/VI vẫn là lỗi chẩn đoán kiểu quan hệ đã được biết đến.

Thứ tự tương tự giữa các thành phố trên toàn quốc đã được sao chép trên profile tham chiếu true-FP32 đã được chứng minh với điểm số hơi khác nhau. Điều đó ủng hộ kết luận rằng thứ tự này không dành riêng cho FP16; nó không ngụ ý các kết quả đầu ra giống hệt byte trên các dtype.

## Khả năng tái tạo

Khởi động Canonical Kaggle CPU-FP16:

```bash
export QDRANT_STORAGE_PATH=/kaggle/working/qdrant-bilingual-search/qdrant-data
bash scripts/kaggle/run-qwen3-transformers-fp16-cpu.sh
```

Trình bao bọc phân giải model read-only từ `/kaggle/input`, thực thi hợp đồng CPU-FP16 được chấp nhận, cho phép tải ngoại tuyến và khởi chạy vòng đời sản xuất-bản demo.

Các lệnh xác minh:

```bash
npm run verify:canonical-config
npm run verify:semantic-index -- 20000
npm run seed:status -- --once --expected 20000
npm test
```

## Những hạn chế đã biết

- CPU demo/runtime di động, không phục vụ GPU có độ trễ thấp.
- Không có người xếp hạng lại.
- Không có truy xuất thưa thớt + dày đặc kết hợp.
- Không có lớp RAG.
- Không có khả năng gieo hạt runtime tự động.
- Chẩn đoán kiểu quan hệ có thể bộc lộ các giới hạn xếp hạng ngay cả khi quá trình xác minh ngữ nghĩa canonical vượt qua.

## Công việc tham khảo

`runtime/true-fp32` giữ lại triển khai CPU true-FP32 dựng sẵn đã được chứng minh dưới dạng branch kỹ thuật/tham chiếu. canonical `v1.0.0` profile vẫn là CPU FP16 vì nó cung cấp khoảng trống bộ nhớ thiết thực hơn trên Kaggle.


## Tài sản release

| Tệp | Mục đích |
| --- | --- |
| `nodejs-qdrant-v1.0.0-production-demo-evidence-20260831T105116Z.zip` | Bằng chứng Kaggle đã làm sạch về runtime, endpoint, acceptance, hệ thống và nguồn source |
| `nodejs-qdrant-v1.0.0-production-demo-evidence-20260831T105116Z.zip.sha256` | Sidecar SHA-256 di động cho evidence ZIP |
| `nodejs-qdrant-bilingual-search-v1.0.0-kaggle-cpu-fp16-production-demo.ipynb` | Notebook canonical sạch, không có output từ source tree của release |
| `nodejs-qdrant-bilingual-search-v1.0.0-kaggle-cpu-fp16-production-demo.ipynb.sha256` | Sidecar SHA-256 cho notebook |
| `nodejs-qdrant-v1.0.0-release-manifest.json` | Danh tính source, hợp đồng runtime và digest artifact ở dạng máy đọc được |
| `nodejs-qdrant-v1.0.0-release-manifest.json.sha256` | Sidecar SHA-256 cho release manifest |

## Tài liệu

- [README.md](../../README.md) — tổng quan về dự án và bắt đầu nhanh
- [README.vi.md](../../README.vi.md) — Tổng quan bằng tiếng Việt
- [Bản demo production](../production-demo.md)
- [Profile Kaggle CPU Transformers FP16](../qwen3-embedding-kaggle-transformers-fp16.md)
- [Kiến trúc](../architecture.md)
- [Tập dữ liệu và seeding](../dataset.md)
- [Dịch](../translation.md)
- [Đang kiểm tra](../testing.md)
- [Danh mục kỹ thuật](../portfolio.md)
